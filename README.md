# Basic UART routines in AVR microcontroller

Example of configuration of UART periferical in C plus a DEBUG interface (a UART too, but in assembly using any I/O) to know what's going 
on in the comunication.


